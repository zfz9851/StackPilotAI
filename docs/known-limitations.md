# 已知限制清单

1. 前端使用 `Vue2.x（第二代 Vue 框架）` 与 `Element-UI（界面组件库）`，属于维护期技术栈，后续建议升级到 Vue3 + Element Plus。
2. 当前未接入自动化端到端测试（E2E），测试报告以人工验证为主。
3. 选型库导入采用“全量覆盖”策略，不做增量合并。
4. 导出文件名使用项目名，若项目名包含特殊文件名字符，浏览器会自动处理；建议使用常规命名。
5. 目前仅支持单机本地 SQLite 文件，不包含多用户并发与权限隔离能力。
